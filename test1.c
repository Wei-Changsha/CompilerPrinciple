int m;
int k;
int ddd[4][6];
int a[3];

int x(){
    int k,l;
    int k;//变量重复定义
    int b[4];
    a = 9 + 4;
    y = 1 * a;
    k=1.2;
    k();//对非函数名采用函数调用形式
    y1();//调用未定义或未声明的函数
    b[5];//数组未定义
    a[1.5];//数组变量的下标不是整型表达式
    ++c;//c 变量未定义,不能对左值表达式进行自增、自减运算
    1++;//对非左值表达式进行自增、自减运算
    return 1;
}
int main(){
    x(6);//函数调用时参数个数不匹配，如实参表达式个数太多、或实参表达式个数太少；

    return 0;
}

